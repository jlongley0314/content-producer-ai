import axios from "axios";

export function signUpUser(params) {
  // TODO: update url with authentication url
  const url = "";
  return axios.post(url, params);
}

export function signInUser(params) {
  // TODO: update url with login url
  const url = "";
  return axios.post(url, params);
}
